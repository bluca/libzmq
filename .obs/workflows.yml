workflow:
  steps:
    - branch_package:
        source_project: network:messaging:zeromq:git-draft
        source_package: libzmq
        target_project: network:messaging:zeromq:ci
release:
  steps:
    - rebuild_package:
        project: home:bluca:branches:network:messaging:zeromq:release-stable
        package: libzmq
  filters:
    event: tag_push
